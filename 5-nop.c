#include "monty.h"

/**
 * mnty_nop - prints nothing
 * @stack_start: pointer to the top of the stack
 * @ln_num: current line in input file;
 * Return: returns nothing
 */
void mnty_nop(stack_t **stack_start, unsigned int ln_num)
{
	(void) ln_num;
	(void) stack_start;
}
